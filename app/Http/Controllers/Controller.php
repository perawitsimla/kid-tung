<?php

namespace App\Http\Controllers;

abstract class Controller
{
    //hee

    //ขั้นตอนการเอาโค้ดของมึงยัดใส่เข้าgitและอัพเดทจากของเพื่อน(ใช้git pull)
    //git pull เพื่ออัพเดทเวอชั่นล่าสุด (จากเพื่อนๆที่ยัดเข้าgit)
    //git add <ตามด้วยตำแหน่งที่อยู่ไฟล์ เช่น app/Http/Controllers/Controller.php>
    //git status เพื่อเช็ค
    //git commit -m "เขียนคำอธิบายว่าทำอะไรไปบ้างในโค้ด"
    //git push เพื่ออัพลง github
}
